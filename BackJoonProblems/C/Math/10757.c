#include <stdio.h>
#define MAX_CAPACITANCY 100000

int main(void)
{

    char a[MAX_CAPACITANCY] = {
        0,
    };
    char b[MAX_CAPACITANCY] = {
        0,
    };
    scanf("%s", a);
    scanf("%s", b);
    
    return 0;
}