#include <stdio.h>
// 1부터 N까지 자연수 중에서 중복 없이 M개를 고른 수열
//한 줄에 하나씩 문제의 조건을 만족하는 수열을 출력한다. 중복되는 수열을 여러 번 출력하면 안되며,
//각 수열은 공백으로 구분해서 출력해야 한다.
//수열은 사전 순으로 증가하는 순서로 출력해야 한다.
int main(void)
{
    int n, m;
    scanf("%d %d", &n, &m);

    //1부터 n까지의 자연수 중에서 중복없이 m개를 골라야 한다.
    //1부터 n까지의 배열에서
    return 0;
}