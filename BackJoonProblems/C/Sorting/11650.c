#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct
{
    int x;
    int y;
} point;

point sort[100001];

void merge(point *arr, int first, int mid, int last)
{
}
int main(void)
{
    int i, t, j;
    scanf("%d", &t);

    return 0;
}
