#include <stdio.h>

int main(void)
{
    int i, n;
    int arr[100000][100000] = {
        0,
    };
    scanf("%d\n", &n);

    for (i = 0; i < n; i++)
    {
        scanf("%d %d", arr[i], )
    }
    return 0;
}