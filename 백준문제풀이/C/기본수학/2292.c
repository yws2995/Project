#include <stdio.h>

int main(void)
{
    int room = 1;
    int num;
    scanf("%d", &num);
    if (num == 1)
    {
        printf("1")
    }
    1씩 뺴면서 6으로 나눠서 몫이 0이 되면 room count를 출력한다.return 0;
}